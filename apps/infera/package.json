{
  "name": "@ideal-frontbase/infera",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --mode dev",
    "build": "vite build --mode prod",
    "build-mock": "vite build --mode mock",
    "build-mock:pages": "pnpm run build-mock && node scripts/build-pages-404.mjs mock",
    "preview": "vite preview",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "typecheck": "tsc --noEmit",
    "lint": "biome check . --no-errors-on-unmatched",
    "format": "biome format . --write --no-errors-on-unmatched",
    "msw:init": "msw init public/ --save",
    "routes:generate": "node scripts/generate-route-tree.mjs"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.12.3",
  "dependencies": {
    "@ideal-frontbase/api-core": "workspace:*",
    "@ideal-frontbase/app-config": "workspace:*",
    "@ideal-frontbase/auth-core": "workspace:*",
    "@ideal-frontbase/confirm": "workspace:*",
    "@ideal-frontbase/error-core": "workspace:*",
    "@ideal-frontbase/hooks-core": "workspace:*",
    "@ideal-frontbase/layout-core": "workspace:*",
    "@ideal-frontbase/mock-core": "workspace:*",
    "@ideal-frontbase/platform-router": "workspace:*",
    "@ideal-frontbase/shared-types": "workspace:*",
    "@ideal-frontbase/table": "workspace:*",
    "@ideal-frontbase/theme-system": "workspace:*",
    "@ideal-frontbase/ui": "workspace:*",
    "@ideal-frontbase/ui-utils": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.0.10"
  },
  "msw": {
    "workerDirectory": [
      "public"
    ]
  }
}
