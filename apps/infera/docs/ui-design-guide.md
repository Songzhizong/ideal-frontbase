# Infera UI/UX 设计规范

> 本文档定义 Infera 的视觉设计语言和交互规范。
> AIAgent 在实现每个页面时**必须**参考本文档，确保全局视觉一致性和高品质 UI/UX。

---

## 一、设计风格定位

| 维度         | 定义                                                                          |
| ------------ | ----------------------------------------------------------------------------- |
| **风格**     | 现代轻盈型 SaaS Dashboard — 清爽、明亮、色彩丰富但克制                        |
| **基调**     | 专业而友好 — 兼具数据密集平台的可信感与宜人的视觉体验                         |
| **浅色优先** | 默认 Light Mode，同时完整支持 Dark Mode                                       |
| **信息密度** | 中高信息密度 — 善用留白、卡片分组和层级对比营造呼吸感                         |
| **色彩策略** | 以纯白/浅灰为底画布，通过**功能色图标**和**彩色状态标签**点缀，让关键信息跳出 |

> **风格对标**：截图中的控制台风格 — 白底卡片 + 彩色图标容器 + 简洁图表 + 柔和阴影。同时参考 Notion、Linear Light、Stripe Dashboard 的视觉气质。

---

## 二、配色体系

### 2.1 基于 Shadcn 语义变量

所有颜色**严格使用 Shadcn 语义化 CSS 变量**，禁止硬编码十六进制。

```
bg-background        — 页面底色（浅灰 #f8f9fa 色调）
bg-card              — 卡片/面板底色（纯白）
bg-muted             — 次要背景（浅灰）
bg-primary           — 主按钮/重点元素
bg-secondary         — 次要按钮
bg-destructive       — 危险操作
text-foreground      — 主文本（深灰/近黑）
text-muted-foreground — 次要文本（中灰）
border-border        — 主边框
border-border/50     — 细分隔线
```

### 2.2 功能色板（Feature Color Palette）

每个功能色提供 **图标容器底色 + 图标色** 的配对，用于指标卡、分类标识等场景。色彩丰富但保持克制——每张卡片/页面同时使用不超过 4-5 种功能色。

| 功能色 | 图标容器底色                          | 图标/文字色        | 典型用途                 |
| ------ | ------------------------------------- | ------------------ | ------------------------ |
| 蓝色   | `bg-blue-50` / `bg-blue-500/10`       | `text-blue-500`    | 项目数、通用信息、进行中 |
| 绿色   | `bg-emerald-50` / `bg-emerald-500/10` | `text-emerald-500` | 在线服务、成功、就绪     |
| 橙色   | `bg-orange-50` / `bg-orange-500/10`   | `text-orange-500`  | 费用/成本、警告          |
| 红色   | `bg-red-50` / `bg-red-500/10`         | `text-red-500`     | 告警、错误、危险操作     |
| 紫色   | `bg-violet-50` / `bg-violet-500/10`   | `text-violet-500`  | 模型/AI 相关、高级功能   |
| 青色   | `bg-cyan-50` / `bg-cyan-500/10`       | `text-cyan-500`    | 数据/流量、辅助信息      |

> **图标容器样式**：`w-10 h-10 rounded-xl flex items-center justify-center` + 对应功能色底色，内部放 Lucide 图标（`w-5 h-5` + 对应功能色图标色）。

### 2.3 状态色彩映射

| 状态                             | 颜色方案                                            | 应用场景           |
| -------------------------------- | --------------------------------------------------- | ------------------ |
| Ready / Succeeded / Active       | `bg-emerald-50 text-emerald-600 border-emerald-200` | 服务就绪、任务成功 |
| Running / Starting / Downloading | `bg-blue-50 text-blue-600 border-blue-200`          | 进行中状态         |
| Pending / Queued                 | `bg-amber-50 text-amber-600 border-amber-200`       | 等待中状态         |
| Failed / Error                   | `bg-red-50 text-red-600 border-red-200`             | 失败/错误          |
| Inactive / Disabled / Canceled   | `bg-muted text-muted-foreground`                    | 停用/取消          |
| Warning                          | `bg-orange-50 text-orange-600 border-orange-200`    | 告警/风险提示      |
| Triggered / Firing               | `bg-red-50 text-red-600 border-red-200 font-medium` | 告警触发中         |
| Resolved / Recovered             | `bg-emerald-50 text-emerald-600 border-emerald-200` | 已恢复             |

### 2.4 环境标签配色

| 环境 | Badge 样式                                           |
| ---- | ---------------------------------------------------- |
| Dev  | `bg-blue-50 text-blue-600 border border-blue-200`    |
| Test | `bg-amber-50 text-amber-600 border border-amber-200` |
| Prod | `bg-red-50 text-red-600 border border-red-200`       |

---

## 三、排版规范

### 3.1 字体

- **正文**：使用项目已配置的 `font-inter`（Inter + 中文回退字体栈）
- **标题**：同正文字体，通过 `font-semibold` / `font-bold` 区分层级
- **代码/ID/数值**：`font-mono`（使用系统 monospace 字体栈）
- 不额外引入 Google Fonts，使用项目现有字体配置

### 3.2 字号层级

| 层级     | 类名                                | 用途             |
| -------- | ----------------------------------- | ---------------- |
| 页面标题 | `text-2xl font-bold tracking-tight` | 页面主标题       |
| 页面描述 | `text-sm text-muted-foreground`     | 页面标题下方描述 |
| 区块标题 | `text-base font-semibold`           | 卡片/区块标题    |
| 表格标题 | `text-sm font-medium`               | 表头文字         |
| 正文     | `text-sm`                           | 表格内容、描述   |
| 辅助文字 | `text-xs text-muted-foreground`     | 时间戳、ID、说明 |
| 指标数值 | `text-3xl font-bold tabular-nums`   | 看板指标大数字   |
| 趋势标注 | `text-xs font-medium`               | 较上月 +12.3%    |

### 3.3 ID 与哈希显示

```
model_version_id: "mv-a1b2c3d4"     → 显示: mv-a1b2c3d4  [📋]
sha256: "abc123def456..."            → 显示: abc123de...   [📋]

样式：font-mono text-xs bg-muted px-1.5 py-0.5 rounded
```

---

## 四、布局与间距

### 4.1 全局布局

```
┌──────────────────────────────────────────────────────┐
│ Topbar (h-14, fixed top, border-b border-border/50)  │
├────────┬─────────────────────────────────────────────┤
│ Sidebar│ Content Area (bg-background)                │
│ (w-60) │ ┌─────────────────────────────────────────┐ │
│ bg-card│ │ PageHeader (py-6 px-6)                  │ │
│ fixed  │ │ Title + Description + Actions            │ │
│ left   │ ├─────────────────────────────────────────┤ │
│        │ │ Main Body (px-6 pb-6)                   │ │
│        │ │ Cards / Tables / Charts / Logs          │ │
│        │ └─────────────────────────────────────────┘ │
└────────┴─────────────────────────────────────────────┘
```

> 页面底色 `bg-background`（浅灰）与卡片底色 `bg-card`（纯白）形成微妙的层级对比。

### 4.2 间距规范

| 区域            | 间距                   |
| --------------- | ---------------------- |
| 页面内边距      | `px-6 py-6`            |
| 区块间距        | `space-y-6`            |
| 卡片/面板内边距 | `p-4` 或 `p-6`         |
| 表格行高        | `h-12`（包含 padding） |
| 按钮组间距      | `gap-2`                |
| 表单字段间距    | `space-y-4`            |
| Tab 与内容间距  | `mt-4`                 |
| 指标卡行间距    | `gap-4` 或 `gap-6`     |

### 4.3 卡片与面板

```css
/* 指标卡片 — 白底 + 细边框 + 柔和阴影 */
bg-card rounded-xl border border-border/50 p-5 shadow-sm

/* 内容面板 — 包含表格/图表的容器 */
bg-card rounded-xl border border-border/50 shadow-sm

/* 悬浮卡片（Popover/Dropdown） */
bg-popover rounded-xl border border-border shadow-md
```

> 统一使用 `rounded-xl` 营造柔和、现代的视觉感受。`shadow-sm` 在浅色背景上提供微妙的立体层次。

---

## 五、组件设计规范

### 5.1 指标卡（MetricCard）

指标卡是仪表盘的核心元素，需要通过**彩色图标容器**让每个指标具有视觉辨识度。支持两种布局变体：

**Layout A：图标在左侧（紧凑型）** — 适合概览页中 4-6 个指标并排

```
┌──────────────────────────────────┐
│  ┌────┐  标签 text-xs              │
│  │icon│  text-muted-foreground     │  ← 彩色图标容器（w-10 h-10 rounded-xl）
│  └────┘                            │
│  8                                 │  ← text-3xl font-bold tabular-nums
│  活跃项目                          │  ← text-xs text-muted-foreground
└──────────────────────────────────┘
```

**Layout B：图标在右侧（信息型）** — 适合需要展示副标题/说明的场景

```
┌──────────────────────────────────┐
│  标签 text-sm            ┌────┐  │  ← 标签在左上
│  副标题/说明              │icon│  │  ← 彩色圆形图标容器（w-10 h-10 rounded-full）
│  text-xs muted           └────┘  │     在右上角
│                                  │
│  1,248                           │  ← text-3xl font-bold tabular-nums
│  或 12/89                        │  ← 可选分数格式
└──────────────────────────────────┘
```

**通用样式：**

```
容器：bg-card rounded-xl border border-border/50 p-5 shadow-sm

图标容器样式：
  圆角方形：w-10 h-10 rounded-xl flex items-center justify-center
  圆形：    w-10 h-10 rounded-full flex items-center justify-center

功能色映射：
  - 用户/项目：bg-blue-50 + text-blue-500 + Users/Server 图标
  - 在线/活跃：bg-emerald-50 + text-emerald-500 + Activity 图标
  - 费用/文件：bg-orange-50 + text-orange-500 + Wallet/FileText 图标
  - 告警/运行：bg-red-50 + text-red-500 + AlertTriangle 图标
  - 任务/时钟：bg-cyan-50 + text-cyan-500 + Clock 图标

趋势标注：▲ text-emerald-500 / ▼ text-red-500 + text-xs font-medium
```

### 5.2 DataTable 表格

- 表头：`bg-muted/50 text-muted-foreground text-xs font-medium uppercase tracking-wider`
- 行：`border-b border-border/50 hover:bg-muted/50 transition-colors`
- 选中行：`bg-primary/5`
- 分页器：底部居右，显示总数 + 每页条数 + 翻页
- **表格外层**：`overflow-x-auto rounded-xl border border-border/50`

### 5.3 StatusBadge 状态徽标

使用柔和的彩色填充背景，文字颜色与背景同色系但更深，整体视觉轻盈不突兀。

```
样式模式：bg-{color}-50 text-{color}-600 border border-{color}-200 rounded-full px-2.5 py-0.5 text-xs font-medium
左侧圆点：w-1.5 h-1.5 rounded-full bg-{color}-500 mr-1.5 （Running 状态带 animate-pulse）

示例：
  Ready   → bg-emerald-50 text-emerald-600 border-emerald-200 + 绿色实心圆点
  Starting → bg-blue-50 text-blue-600 border-blue-200 + 蓝色脉冲圆点
  触发中   → bg-red-50 text-red-600 border-red-200 + font-medium
  已恢复   → bg-emerald-50 text-emerald-600 border-emerald-200
```

### 5.4 操作链接

表格或列表中的操作链接使用 `text-primary hover:underline` 或 `text-blue-500 hover:underline`，如截图中的"查看全部"。

### 5.5 彩色进度条（ProgressBar）

根据百分比动态变色的使用率/进度指示器：

```
使用率  ████████░░░░░░░░  68%

颜色阈值：
  0-50%  → bg-emerald-500（绿色，健康）
  50-80% → bg-amber-500（黄色，中等）
  80%+   → bg-red-500（红色，高负载/警告）

样式：
  轨道：h-1.5 rounded-full bg-muted
  填充：h-1.5 rounded-full transition-all duration-300
  百分比文字：text-sm font-medium tabular-nums（右对齐）
```

### 5.6 模块状态卡（ModuleStatusCard）

展示服务/模块运行状态的卡片，包含名称、描述、状态徽标、使用率进度条和最后更新时间。

```
┌──────────────────────────────────────┐
│  身份识别与访问控制       [运行中]    │  ← 名称 text-sm font-semibold + StatusBadge
│  用户认证、权限管理、单点登录         │  ← 描述 text-xs text-muted-foreground
│                                      │
│  使用率  ████████░░░  85%            │  ← 彩色进度条 + 百分比
│                                      │
│  最后更新: 2024/01/28 18:30:00       │  ← text-xs text-muted-foreground
└──────────────────────────────────────┘

容器：bg-card rounded-xl border border-border/50 p-5 shadow-sm
布局：grid grid-cols-2 gap-4（2列网格排列多个模块卡）
状态变体：
  运行中 → bg-emerald-50 text-emerald-600 (无边框，填充式 pill)
  维护中 → bg-amber-50 text-amber-600
  已停止 → bg-muted text-muted-foreground
```

### 5.7 活动时间线（ActivityTimeline）

展示最近操作记录的列表，每条记录左侧有**彩色圆形图标**标识操作类型。

```
┌──────────────────────────────────────────────────────┐
│  最近活动                                            │
│  系统最新的操作记录和活动日志                          │
├──────────────────────────────────────────────────────┤
│  🔵  用户登录系统   登录     张三  2024/01/28 19:45  │
│  🟢  上传文件 x.pdf  文件    李四  2024/01/28 19:30  │
│  🟣  定时任务执行完成 任务   系统  2024/01/28 19:15  │
│  🔴  发送系统公告    通知    王五  2024/01/28 19:00  │
└──────────────────────────────────────────────────────┘

每行结构：
  圆形图标容器：w-8 h-8 rounded-full flex items-center justify-center + 功能色
  操作描述：text-sm font-medium
  类型标签：text-xs bg-muted rounded px-1.5 py-0.5
  操作人：text-xs text-muted-foreground
  时间戳：text-xs text-muted-foreground

图标色映射：
  登录/认证 → bg-blue-50 text-blue-500
  文件操作 → bg-emerald-50 text-emerald-500
  任务执行 → bg-violet-50 text-violet-500
  通知/公告 → bg-red-50 text-red-500
  系统事件 → bg-cyan-50 text-cyan-500
```

### 5.8 通知计数徽标（NotificationBadge）

用于未读消息、待处理项等的数字标识。

```
样式：
  小数字（≤99）：min-w-5 h-5 rounded-full bg-red-500 text-white text-xs
                 font-medium flex items-center justify-center px-1.5
  大数字（99+）：显示 "99+"，自动加宽
  无数字（圆点）：w-2 h-2 rounded-full bg-red-500

常见用法：
  导航铃铛图标右上角偏移：absolute -top-1 -right-1
  列表项行内：inline-flex ml-auto（如 "未读通知 [23]"）
```

### 5.9 Wizard 向导

```
┌─────────────────────────────────────────────────────┐
│ Step 1 ──●── Step 2 ──○── Step 3 ──○── Step 4 ──○  │
│ 选择模型     训练数据     训练配置     确认提交       │
├─────────────────────────────────────────────────────┤
│                                                     │
│                 步骤内容区                            │
│                 (min-h-[400px])                      │
│                                                     │
├─────────────────────────────────────────────────────┤
│                          [上一步]  [下一步/提交]      │
└─────────────────────────────────────────────────────┘

步骤条样式：
- 已完成：bg-primary text-primary-foreground
- 当前步：border-2 border-primary text-primary
- 未执行：bg-muted text-muted-foreground
- 连接线：h-0.5 bg-primary（已完成）/ bg-muted（未完成）
```

### 5.10 EmptyState 空态

```
┌─────────────────────────────────────┐
│          ┌────────┐                 │
│          │  Icon  │ (48x48, muted)  │
│          └────────┘                 │
│    主标题（text-lg font-semibold）   │
│    描述文案（text-muted-foreground） │
│                                     │
│    [主要操作 CTA]  [次要操作]        │
└─────────────────────────────────────┘

容器：flex flex-col items-center justify-center py-16 text-center
```

### 5.11 LogViewer 日志查看器

```
┌────────────────────────────────────────────────┐
│ 🔍 搜索  │ Level ▾ │ Revision ▾ │ 暂停 │ 下载  │  过滤条
├────────────────────────────────────────────────┤
│ 2025-01-01 12:00:01  INFO  Starting server...  │
│ 2025-01-01 12:00:02  INFO  Model loaded         │  虚拟滚动列表
│ 2025-01-01 12:00:03  WARN  High memory usage    │  WARN 行 bg-amber-50
│ 2025-01-01 12:00:04  ERROR Connection timeout   │  ERROR 行 bg-red-50
└────────────────────────────────────────────────┘

样式：
- 背景：bg-muted/30 (浅灰底，保持整体浅色基调)
- 深色模式下可切换为 bg-[oklch(0.15_0_0)] (终端感)
- 字体：font-mono text-xs
- 时间戳：text-muted-foreground
- Level 颜色：INFO=text-blue-500, WARN=text-amber-500, ERROR=text-red-500
```

### 5.12 Playground 对话区

```
┌────────────────────────────┬──────────────┐
│ Chat Area                  │ Parameters   │
│ ┌────────────────────────┐ │ Model: ...   │
│ │ User: What is...       │ │ Temp: 0.7    │
│ │ AI: The answer is...   │ │ Top P: 0.9   │
│ └────────────────────────┘ │ Max: 2048    │
│                            │ Stream: ✓    │
│ ┌────────────────────────┐ │              │
│ │ 输入框       [发送]    │ │ Token Usage  │
│ └────────────────────────┘ │ P:123 C:456  │
├────────────────────────────┤              │
│ ▶ Raw Request / Response   │              │
└────────────────────────────┴──────────────┘

用户消息气泡：bg-primary/10 rounded-lg p-3
AI 消息气泡：bg-muted rounded-lg p-3
流式输出：逐字符渲染 + 闪烁光标动画
```

---

## 六、交互与动效规范

### 6.1 过渡动效

| 场景             | 动效                                   | Duration |
| ---------------- | -------------------------------------- | -------- |
| 按钮 hover       | `transition-colors duration-150`       | 150ms    |
| 卡片 hover       | `transition-all duration-200`          | 200ms    |
| 弹窗/抽屉出现    | fade-in + slide-up / slide-right       | 200ms    |
| 弹窗/抽屉消失    | fade-out + slide-down / slide-left     | 150ms    |
| Tab 切换         | fade 内容过渡                          | 150ms    |
| 页面路由切换     | fade                                   | 100ms    |
| Loading skeleton | `animate-pulse`                        | 持续     |
| Running 状态圆点 | `animate-pulse`                        | 持续     |
| Toast 通知       | slide-in from top-right → auto-dismiss | 显示 5s  |
| 删除行消失       | 行高收缩 + 透明度变为 0                | 300ms    |

### 6.2 Hover 与焦点

- **所有可点击元素**：`cursor-pointer`
- **表格行 hover**：`hover:bg-muted/50`
- **卡片 hover**：`hover:border-border hover:shadow-md transition-all duration-200`
- **按钮 hover**：使用 Shadcn 内建 hover 状态
- **焦点状态**：`focus-visible:ring-2 focus-visible:ring-ring`

### 6.3 反馈机制

| 场景       | 反馈方式                                 |
| ---------- | ---------------------------------------- |
| 表单提交中 | 按钮 spinner + disabled                  |
| 数据加载中 | Skeleton 骨架屏（表格行/卡片）           |
| 刷新中     | 中心 spinner（不切走已有内容）           |
| 操作成功   | Toast（含"查看审计记录"链接）            |
| 操作失败   | Toast (destructive variant) + 可展开详情 |
| 复制成功   | Toast mini：「已复制」                   |
| 权限不足   | 按钮 disabled + tooltip 说明原因         |

---

## 七、图表设计规范

### 7.1 图表库

使用 **Recharts**（React 原生，与 Shadcn Charts 一致）

### 7.2 图表色板

图表使用明确、可辨识的色彩，与页面浅色基调形成适度对比：

| 序号 | 用途        | 颜色方案                   | 说明               |
| ---- | ----------- | -------------------------- | ------------------ |
| 1    | 主线/主系列 | `hsl(var(--chart-1))` 蓝色 | Token 用量、QPS 等 |
| 2    | 辅线/次系列 | `hsl(var(--chart-2))` 橙色 | 成本、延迟等       |
| 3    | 第三系列    | `hsl(var(--chart-3))` 绿色 | 成功率等           |
| 4    | 第四系列    | `hsl(var(--chart-4))` 紫色 | GPU 利用率等       |
| 5    | 第五系列    | `hsl(var(--chart-5))` 青色 | 辅助指标           |

### 7.3 图表样式

- **面积填充**：主色 8-15% 透明度渐变（顶部 → 底部透明），在白色背景上提供柔和的视觉厚度
- **线条**：`strokeWidth: 2`，圆润线帽 `strokeLinecap: "round"`
- **网格线**：`stroke: hsl(var(--border))` + `opacity: 0.3`，仅水平网格，无纵向，保持简洁
- **坐标轴文字**：`text-xs text-muted-foreground`
- **Tooltip**：`bg-card border border-border rounded-xl shadow-lg p-3`
- **图例**：底部居中，`text-xs`

### 7.4 Metrics Tab 布局

```
┌──────────────────────────────────────────────────┐
│ [1h] [6h] [24h] [7d] [自定义]  粒度▾  Revision▾ │  控制条
├────────┬────────┬────────┬────────┬───────┬──────┤
│成功率  │P95     │TTFT    │Tokens/s│GPU%   │冷启动│  指标卡行
│99.2%   │45ms    │120ms   │1.2K    │78%    │3     │
├────────┴────────┴────────┴────────┴───────┴──────┤
│ ┌──────────────┐ ┌──────────────┐                │
│ │  QPS 折线图  │ │ 延迟 P95/P99 │                │  2列图表网格
│ └──────────────┘ └──────────────┘                │
│ ┌──────────────┐ ┌──────────────┐                │
│ │ 成功/错误率  │ │ TTFT / TPOT  │                │  grid grid-cols-2
│ └──────────────┘ └──────────────┘                │  gap-4
│ ┌──────────────┐ ┌──────────────┐                │
│ │ Tokens/sec   │ │ GPU Util/Mem │                │
│ └──────────────┘ └──────────────┘                │
└──────────────────────────────────────────────────┘
```

---

## 八、页面布局线框（关键页面）

### 8.1 租户概览/看板

```
┌──────────────────────────────────────────────────────┐
│ PageHeader                                           │
│ 控制台                                               │
│ Infera 平台概览                                      │
├──────────┬──────────┬──────────┬──────────────────────┤
│ 🔵项目数 │ 📊在线   │ 💰本月   │ 🔴活跃告警          │  彩色图标指标卡
│ 8        │ 4        │ ¥74,220  │ 3                   │
│ 活跃项目 │ 共8个服务│ 较上月+12%│ 5条总告警           │
├──────────┴──────────┴──────────┴──────────────────────┤
│ ┌────────────────────────┐ ┌────────────────────────┐│
│ │ Token 用量趋势 (蓝线)  │ │ 每日成本 (橙线)       ││  白底图表卡
│ └────────────────────────┘ └────────────────────────┘│
│ ┌────────────────────────┐ ┌────────────────────────┐│
│ │ 推理服务状态           │ │ 最近告警               ││
│ │ nlp ● Ready 245 QPS   │ │ ⚠ 错误率异常  触发中   ││  列表卡
│ │ chat ● Ready 1200 QPS │ │ ⚠ P95延迟    触发中   ││
│ │ vision ○ Starting      │ │ ✅ 预算80%   已恢复   ││
│ └────────────────────────┘ └────────────────────────┘│
└──────────────────────────────────────────────────────┘
```

### 8.2 服务列表页

```
┌──────────────────────────────────────────────────────┐
│ PageHeader                                           │
│ Inference Services                    [+ 创建服务]   │
│ 管理和监控你的推理服务端点                             │
├──────────────────────────────────────────────────────┤
│ 筛选条: [Env▾] [状态▾] [Runtime▾] [模型▾] [🔍搜索]  │
├──────────────────────────────────────────────────────┤
│ ┌──────────────────────────────────────────────────┐ │
│ │ DataTable                                        │ │
│ │ Name  Env  State  Endpoint  Model  Runtime ...   │ │
│ │ ───────────────────────────────────────────────── │ │
│ │ svc-a  Prod ● Ready  https://...  mv-123  vLLM   │ │
│ │ svc-b  Dev  ○ Starting  —    mv-456  TGI         │ │
│ │ ...                                              │ │
│ │                              1-10 of 42  < 1 2 > │ │
│ └──────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────┘
```

### 8.3 服务详情页

```
┌──────────────────────────────────────────────────────┐
│ Header                                               │
│ svc-production-gpt │ Prod │ ● Ready                  │
│ Endpoint: https://api.infera.ai/v1  [📋]             │
│ Desired: [Active ◉ / Inactive ○]                     │
│ [Deploy Revision] [Edit Traffic] [Rollback] [▶ Play] │
├──────────────────────────────────────────────────────┤
│ 状态步骤条:                                            │
│ ✓ Pending → ✓ Downloading → ✓ Starting → ● Ready    │
├──────────────────────────────────────────────────────┤
│ [Overview] [Revisions] [Metrics] [Logs] [Play] [Set] │
│ ─────────                                            │
│ Tab 内容区域...                                       │
└──────────────────────────────────────────────────────┘
```

### 8.4 微调任务详情页

```
┌──────────────────────────────────────────────────────┐
│ Header                                               │
│ fine-tune-job-001 │ ● Running │ 45%                  │
│ [取消] [克隆]                                         │
├──────────────────────────────────────────────────────┤
│ [Overview] [Metrics] [Logs] [Artifacts] [Audit]      │
│ ────────── ─────────                                 │
│                                                      │
│ Metrics Tab:                                         │
│ ┌──────────────────────────────────────────────────┐ │
│ │        Loss 曲线                                 │ │
│ │  2.5 ┐                                           │ │
│ │      │\                                          │ │
│ │  1.0 │  \___________                             │ │
│ │      └──────────────── Steps                     │ │
│ └──────────────────────────────────────────────────┘ │
│ ┌──────────────────────────────────────────────────┐ │
│ │        Learning Rate 曲线                        │ │
│ └──────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────┘
```

---

## 九、响应式设计

| 断点          | 布局调整                                  |
| ------------- | ----------------------------------------- |
| `≥1440px`     | 完整布局（Sidebar 展开 + 图表 2 列）      |
| `1024-1439px` | Sidebar 收缩为图标模式                    |
| `768-1023px`  | Sidebar 隐藏（Hamburger 触发），图表单列  |
| `<768px`      | 不作为主要适配目标（企业 SaaS，桌面优先） |

---

## 十、深色/浅色模式

- **浅色优先**：默认浅色模式，同时完整支持深色模式
- 使用 Shadcn 主题变量自动切换，**无需手动指定 dark: 前缀**
- 所有自定义样式不得使用硬编码颜色
- 功能色板（§2.2）在深色模式下自动适配：`bg-{color}-50` → `bg-{color}-500/10`，`text-{color}-600` → `text-{color}-400`
- 测试交付前**必须切换两种模式**验证对比度

---

## 十一、无障碍（Accessibility）

- 所有图片/图标必须有 `alt` / `aria-label`
- 表单字段必须有关联 `<label>`
- 颜色不能作为唯一信息载体（配合图标/文字）
- 键盘导航可用（Tab / Enter / Escape）
- 动效尊重 `prefers-reduced-motion`

---

## 十二、图标规范

- **图标库**：Lucide React（与 Shadcn 一致）
- **尺寸**：导航图标 `w-5 h-5`，行内图标 `w-4 h-4`，小图标 `w-3.5 h-3.5`
- **指标卡图标容器**：`w-10 h-10 rounded-xl` + 功能色底色 + `w-5 h-5` 图标
- **禁止使用 emoji 作为 UI 图标**
- **状态圆点**：`w-1.5 h-1.5 rounded-full` + 对应状态色
