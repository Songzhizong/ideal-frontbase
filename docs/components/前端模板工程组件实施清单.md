# 前端模板工程组件实施清单

> **版本**：v1.0 · **日期**：2026-02-15 · **关联文档**：[前端模板工程组件完整性分析报告](前端模板工程组件完整性分析报告.md)

**状态说明**：`⬜ 待开始` · `🔵 进行中` · `✅ 已完成` · `⏸️ 暂缓`

---

## 阶段一：P0 模板核心能力补齐

### 1. Copy（复制能力）

| 属性     | 说明                                                                                                |
|:-------|:--------------------------------------------------------------------------------------------------|
| 状态     | ✅ 已完成                                                                                             |
| 目标路径   | `packages/hooks-core/use-copy.ts` · `packages/ui/copy-button.tsx`                                 |
| 沉淀来源   | 17 处分散 `navigator.clipboard.writeText()` 调用                                                       |
| 交付件    | `useCopy` Hook + `CopyButton` 组件 + `CopyText` 组件                                                  |
| API 要点 | `useCopy()` 返回 `{ copy, copied }`；`CopyButton` 支持 `value`/`onCopy`/`timeout`；`CopyText` 显示文本+复制图标 |
| 验收要点   | ① 复制成功后短暂反馈（图标切换或 toast） ② 支持暗色模式 ③ 键盘可访问                                                         |
| 依赖     | 无                                                                                                 |

---

### 2. DescriptionList（描述列表）

| 属性     | 说明                                                                                  |
|:-------|:------------------------------------------------------------------------------------|
| 状态     | ✅ 已完成                                                                               |
| 目标路径   | `packages/ui/description-list.tsx`                                                  |
| 沉淀来源   | 无现有业务组件，全新开发                                                                        |
| 交付件    | `DescriptionList` + `DescriptionItem`                                               |
| API 要点 | 支持 `column` 布局（1/2/3 列）、`vertical`/`horizontal` 方向、`bordered` 变体、支持 `items` 数组式 API |
| 验收要点   | ① 响应式列数自适应 ② 空值友好显示 ③ 兼容暗色模式                                                        |
| 依赖     | 无                                                                                   |

---

### 3. Statistic / StatCard（统计指标卡）

| 属性     | 说明                                                                                             |
|:-------|:-----------------------------------------------------------------------------------------------|
| 状态     | ✅ 已完成                                                                                          |
| 目标路径   | `packages/ui/statistic.tsx` · `packages/ui/stat-card.tsx`                                      |
| 沉淀来源   | `apps/infera` 和 `apps/nexus` 中多处 Dashboard 卡片样式                                                |
| 交付件    | `Statistic`（数值展示） + `StatCard`（带卡片容器、趋势、图标）                                                    |
| API 要点 | `value`/`label`/`prefix`/`suffix`/`trend`（`up`/`down`/`neutral`）/`trendValue`/`icon`/`loading` |
| 验收要点   | ① 数值格式化（千分位、百分比等） ② 趋势色彩语义正确 ③ 支持 skeleton 态                                                   |
| 依赖     | 无                                                                                              |

---

### 4. Result / State（统一结果态）

| 属性     | 说明                                                                  |
|:-------|:--------------------------------------------------------------------|
| 状态     | ✅ 已完成                                                               |
| 目标路径   | `packages/ui/result.tsx`                                            |
| 沉淀来源   | `packages/error-core` 已有错误页，需扩展语义                                   |
| 交付件    | `Result` 组件，覆盖 `success`/`error`/`warning`/`info`/`403`/`404`/`500` |
| API 要点 | `status`/`title`/`subtitle`/`icon`（自定义）/`extra`（操作按钮区域）             |
| 验收要点   | ① 内置语义插图或图标 ② 与 `error-core` 复用一致 ③ 支持自定义插图                         |
| 依赖     | `packages/error-core`（复用已有错误语义）                                     |

---

### 5. Upload（通用上传）

| 属性     | 说明                                                                                                    |
|:-------|:------------------------------------------------------------------------------------------------------|
| 状态     | ✅ 已完成                                                                                                 |
| 目标路径   | `packages/ui/upload.tsx` · `packages/hooks-core/use-upload.ts`                                        |
| 沉淀来源   | `apps/nexus/src/features/file-management/` 上传组件族                                                      |
| 交付件    | `Upload`（触发器）+ `UploadDragger`（拖拽区）+ `useUpload` Hook                                                 |
| API 要点 | `accept`/`multiple`/`maxSize`/`maxCount`/`onUpload`（返回 Promise）/`fileList`/`onRemove`；Hook 管理队列、进度、重试 |
| 验收要点   | ① 拖拽上传 ② 进度条与重试 ③ 文件类型/大小校验 ④ 受控/非受控                                                                  |
| 依赖     | Copy（上传完成后复制链接场景）                                                                                     |

---

### 6. Wizard / Steps（流程向导）

| 属性     | 说明                                                                                                          |
|:-------|:------------------------------------------------------------------------------------------------------------|
| 状态     | ✅ 已完成                                                                                                       |
| 目标路径   | `packages/ui/steps.tsx` · `packages/ui/wizard.tsx`                                                          |
| 沉淀来源   | 5 套业务向导（fine-tuning / services / datasets / models / evaluation）                                            |
| 交付件    | `Steps`（步骤条）+ `Wizard`（步骤容器，管理上下步/验证/提交）                                                                    |
| API 要点 | `Steps`: `current`/`items`/`direction`（横/竖）/`status`；`Wizard`: `steps` 数组、`onFinish`、单步 `validation`、支持异步校验 |
| 验收要点   | ① 步骤间表单状态保持 ② 异步验证支持 ③ 响应式布局 ④ 与 Form 体系打通                                                                  |
| 依赖     | 无                                                                                                           |

---

## 阶段二：P1 结构化数据与详情体验

### 7. Timeline

| 属性     | 说明                                                          |
|:-------|:------------------------------------------------------------|
| 状态     | ✅ 已完成                                                       |
| 目标路径   | `packages/ui/timeline.tsx`                                  |
| 交付件    | `Timeline` + `TimelineItem`                                 |
| API 要点 | `mode`（`left`/`right`/`alternate`）、`dot`（自定义节点）、`color` 语义色 |
| 验收要点   | ① 支持自定义节点图标 ② 交替布局 ③ 可折叠长列表                                 |
| 依赖     | 无                                                           |

---

### 8. Tree

| 属性     | 说明                                                                                       |
|:-------|:-----------------------------------------------------------------------------------------|
| 状态     | ✅ 已完成                                                                                    |
| 目标路径   | `packages/ui/tree.tsx`                                                                   |
| 交付件    | `Tree` 组件                                                                                |
| API 要点 | `treeData`/`checkedKeys`/`expandedKeys`/`onSelect`/`onCheck`/`draggable`/`virtual`（虚拟滚动） |
| 验收要点   | ① 大数据量虚拟滚动 ② 拖拽排序 ③ 复选/单选模式 ④ 键盘导航                                                       |
| 依赖     | 无                                                                                        |

---

### 9. TreeSelect

| 属性     | 说明                                                    |
|:-------|:------------------------------------------------------|
| 状态     | ✅ 已完成                                                 |
| 目标路径   | `packages/ui/tree-select.tsx`                         |
| 交付件    | `TreeSelect` 组件                                       |
| API 要点 | 继承 Tree 数据结构 + Select 交互模式、支持 `multiple`/`searchable` |
| 验收要点   | ① 搜索过滤 ② 多选标签展示 ③ 受控/非受控                              |
| 依赖     | Tree、Popover                                          |

---

### 10. Cascader

| 属性     | 说明                                                                        |
|:-------|:--------------------------------------------------------------------------|
| 状态     | ✅ 已完成                                                                     |
| 目标路径   | `packages/ui/cascader.tsx`                                                |
| 交付件    | `Cascader` 组件                                                             |
| API 要点 | `options`（嵌套）/`value`/`onChange`/`loadData`（异步加载）/`multiple`/`searchable` |
| 验收要点   | ① 异步加载子节点 ② 搜索匹配高亮 ③ 路径展示                                                 |
| 依赖     | Popover                                                                   |

---

### 11. Transfer

| 属性     | 说明                                                             |
|:-------|:---------------------------------------------------------------|
| 状态     | ✅ 已完成                                                          |
| 目标路径   | `packages/ui/transfer.tsx`                                     |
| 交付件    | `Transfer` 组件                                                  |
| API 要点 | `dataSource`/`targetKeys`/`onChange`/`searchable`/`renderItem` |
| 验收要点   | ① 双栏穿梭 ② 搜索过滤 ③ 全选/反选 ④ 自定义渲染项                                 |
| 依赖     | Checkbox                                                       |

---

### 12. Image（增强图片）

| 属性     | 说明                                                    |
|:-------|:------------------------------------------------------|
| 状态     | ✅ 已完成                                                 |
| 目标路径   | `packages/ui/image.tsx`                               |
| 交付件    | `Image` 组件                                            |
| API 要点 | `src`/`fallback`/`preview`（点击放大）/`lazy`/`placeholder` |
| 验收要点   | ① 加载失败占位图 ② 懒加载 ③ 点击预览大图 ④ 图片组预览                      |
| 依赖     | Dialog（预览弹层）                                          |

---

### 13. TagInput / MultiInput

| 属性     | 说明                                                                  |
|:-------|:--------------------------------------------------------------------|
| 状态     | ✅ 已完成                                                               |
| 目标路径   | `packages/ui/tag-input.tsx`                                         |
| 交付件    | `TagInput` 组件                                                       |
| API 要点 | `value: string[]`/`onChange`/`separator`（分隔符触发）/`max`/`validateTag` |
| 验收要点   | ① 输入分隔符自动成标签 ② 删除键移除末尾标签 ③ 粘贴批量导入 ④ 与 Form 集成                       |
| 依赖     | Badge、Input                                                         |

---

### 14. Skeleton 页面级预设

| 属性     | 说明                                                                   |
|:-------|:---------------------------------------------------------------------|
| 状态     | ✅ 已完成                                                                |
| 目标路径   | `packages/ui/skeleton-presets.tsx`                                   |
| 交付件    | `TableSkeleton` + `CardSkeleton` + `FormSkeleton` + `DetailSkeleton` |
| API 要点 | `rows`/`columns`（表格预设）、`cards`（卡片数量）                                 |
| 验收要点   | ① 基于现有 Skeleton 组合 ② 尺寸可配置 ③ 动画一致                                    |
| 依赖     | Skeleton                                                             |

---

## 阶段三：P2 体验增强与治理

### 15. Anchor

| 属性     | 说明                                  |
|:-------|:------------------------------------|
| 状态     | ✅ 已完成                               |
| 目标路径   | `packages/ui/anchor.tsx`            |
| 交付件    | `Anchor` + `AnchorLink`             |
| API 要点 | `items`/`offset`/`affix`/`onChange` |
| 验收要点   | ① 滚动高亮当前锚点 ② 固钉模式 ③ 平滑滚动            |
| 依赖     | 无                                   |

---

### 16. BackTop

| 属性     | 说明                                       |
|:-------|:-----------------------------------------|
| 状态     | ✅ 已完成                                    |
| 目标路径   | `packages/ui/back-top.tsx`               |
| 交付件    | `BackTop` 组件                             |
| API 要点 | `target`（滚动容器）/`visibleHeight`/`onClick` |
| 验收要点   | ① 滚动显隐动画 ② 自定义触发容器 ③ 无障碍                 |
| 依赖     | 无                                        |

---

### 17. Tour

| 属性     | 说明                                            |
|:-------|:----------------------------------------------|
| 状态     | ✅ 已完成                                         |
| 目标路径   | `packages/ui/tour.tsx`                        |
| 交付件    | `Tour` + `TourStep`                           |
| API 要点 | `steps`（目标元素 + 描述）/`open`/`onClose`/`current` |
| 验收要点   | ① 遮罩高亮目标元素 ② 步骤导航 ③ 键盘可退出                     |
| 依赖     | Popover                                       |

---

### 18. Watermark

| 属性     | 说明                                               |
|:-------|:-------------------------------------------------|
| 状态     | ✅ 已完成                                            |
| 目标路径   | `packages/ui/watermark.tsx`                      |
| 交付件    | `Watermark` 组件                                   |
| API 要点 | `content`（文字/图片）/`rotate`/`gap`/`opacity`/`font` |
| 验收要点   | ① Canvas 渲染水印 ② 防删除（MutationObserver）③ 多行文字      |
| 依赖     | 无                                                |

---

### 19. QRCode

| 属性     | 说明                                                                   |
|:-------|:---------------------------------------------------------------------|
| 状态     | ✅ 已完成                                                                |
| 目标路径   | `packages/ui/qr-code.tsx`                                            |
| 交付件    | `QRCode` 组件                                                          |
| API 要点 | `value`/`size`/`icon`/`color`/`errorLevel`/`status`（loading/expired） |
| 验收要点   | ① 嵌入 Logo ② 过期/刷新态 ③ 下载                                              |
| 依赖     | 三方库（如 `qrcode`）                                                      |

---

### 20. Mentions / Rate / ColorPicker

| 属性   | 说明                                                                                   |
|:-----|:-------------------------------------------------------------------------------------|
| 状态   | ✅ 已完成                                                                                |
| 目标路径 | `packages/ui/mentions.tsx` · `packages/ui/rate.tsx` · `packages/ui/color-picker.tsx` |
| 交付件  | 三个独立组件                                                                               |
| 验收要点 | 视实际业务需求确定优先级和 API 范围                                                                 |
| 依赖   | 按需引入                                                                                 |

---

## 进度看板

| 阶段     | 总数     | ✅ 完成  | 🔵 进行中 | ⬜ 待开始  | 完成率    |
|:-------|:-------|:------|:-------|:-------|:-------|
| P0     | 6      | 6     | 0      | 0      | 100%   |
| P1     | 8      | 8     | 0      | 0      | 100%   |
| P2     | 6      | 6     | 0      | 0      | 100%   |
| **总计** | **20** | **20** | **0**  | **0** | **100%** |
